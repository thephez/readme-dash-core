language: python

# Makes Travis skip looking for requirements.txt
install: pip --version

before_script:
  - rm -rf export/
  - mkdir -p export/markdown export/full-json export/html
  - cd export

script:
  - ../scripts/download-project-files.py

after_script:
  - cd ..
  - scripts/push.sh
